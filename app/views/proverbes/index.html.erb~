<h1>جميع المستخدمبن</h1>

<%= will_paginate :previous_label => 'السابق', :next_label => 'التالي', :class => 'pager' %>

<ul class="users">
  <% @proverbes.each do |proverbe| %>
    <li>
      
        <span class="user">
    		<%= link_to proverbe.user.name %>
        </span>
    <span class="corps"><%= proverbe.Corps %></span>
  <span class="timestamp">
    أرسل من حوالي <%= time_ago_in_words(feed_item.created_at)%>.
  </span>
  <% if current_user?(proverbe.user) %>
    <%= link_to "حذف", proverbe, :method =>  :delete,
                                     :confirm=> "هل أنت متأكد",
                                     :title=>    proverbe.Corps %>


   <%= link_to "تحديث المثل", edit_proverbe_path(proverbe) %>
  <% end %>
	
    </li>
  <% end %>
</ul>

<%= will_paginate :previous_label => 'السابق', :next_label => 'التالي' , :class => 'pager'%>
