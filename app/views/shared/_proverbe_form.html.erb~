<%= form_for(@proverbe) do |f| %>
  <%= render 'shared/error_messages', :object=> f.object %>
  <div class="field">
    <%= f.text_area :content, :placeholder=> "أدخل مثل أو حكمة جديدة" %>
  </div>
  <%= f.submit "أرسل", :class=> "btn btn-large btn-primary" %>
<% end %>
