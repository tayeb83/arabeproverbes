<%  "Edit user" %> 
<h1>تحديث الحساب</h1>

<div class="row">
  <div class="span6 offset3">
  <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages', :object=> f.object %>

       <%= f.label :"اسم المستخدم" %>
      <%= f.text_field :name %>

      <%= f.label :"البريدالالكتروني" %>
      <%= f.text_field :email %>

      <%= f.label :"كلمة السر" %>
      <%= f.password_field :password %>

      <%= f.label :"تأكيد كلمة السر" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "حفظ التغيرات", :class=> "btn btn-large btn-primary" %>
    <% end %>

    <%= gravatar_for @user %>
    <a href="http://gravatar.com/emails">change</a>
  </div>
</div>
